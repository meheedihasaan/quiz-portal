#Server Configuration
server.port = 8081

#Datasource Configuration
spring.datasource.url = jdbc:postgresql://dev-database.cqjme6mkitr1.us-east-1.rds.amazonaws.com:5432/quiz_portal
spring.datasource.username = postgres
spring.datasource.password = postgres

#JPA Configuration
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format-sql = true

#To check if the database connection is established or not
logging.level.com.zaxxer.hikari= OFF

#Logging Config
logging.file.path = @project.artifactId@

#---- Logging Config ----#
projectRoot = @project.basedir@
logging.config = ${projectRoot}/src/main/resources/logback-production.xml